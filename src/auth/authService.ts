import supabase from "../database/supabaseClient.js";

async function register(userData) {
    // Paso 1: ¿Verificar si el email ya existe?
    // Paso 2: ¿Hashear la contraseña?
    // Paso 3: ¿Insertar en la DB?
    // Paso 4: ¿Qué devolver?
}